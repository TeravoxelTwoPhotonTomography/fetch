/*
 * AlazarTech SDK Utilities
 * ========================
 *
 * Notes
 * -----
 *  
 *  Currently don't have any "real" content in here...the Error2Text function
 *  I added is actually implemented in their API.  I'll probably need to write
 *  some utilities at some point though, so...
 *
 */
#include <AlazarError.h>

// Ack.  Use AlazarErrorToText Instead
const char* AlazarTranslateErrorCode(RETURN_CODE sts);
